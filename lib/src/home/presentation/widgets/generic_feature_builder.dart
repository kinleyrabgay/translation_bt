import 'package:flutter/material.dart';

class GenericFeatureBuilder extends StatelessWidget {
  const GenericFeatureBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
