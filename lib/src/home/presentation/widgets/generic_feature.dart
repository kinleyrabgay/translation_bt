import 'package:flutter/material.dart';

class GenericFeature extends StatelessWidget {
  const GenericFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
